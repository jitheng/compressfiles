# ── Vercel Blob (required for large file uploads > 4.5 MB in production) ──────
#
# Get this token from: Vercel Dashboard → Storage → Blob → your store → .env.local
# Add it to: Vercel Dashboard → Project → Settings → Environment Variables
#
# Without this token, the app falls back to legacy direct multipart upload
# which is limited to ~4 MB (Vercel's hard infrastructure limit).
#
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxx
